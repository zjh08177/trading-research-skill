# Python caches
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/

# Run artifacts — contain LIVE ACCOUNT DATA (positions, $ amounts) + reports. Never commit.
runs/
reports/

# Browser-automation (playwright-mcp) scratch logs
.playwright-mcp/

# Skill-owned venv (bootstrap: scripts/setup_venv.sh)
.venv/
