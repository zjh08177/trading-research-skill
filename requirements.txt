# Runtime deps for the trading-research skill — vendor CLIs, batch drivers, tests.
#
# CANONICAL INTERPRETER: the skill-owned venv at <SKILL_DIR>/.venv
# (bootstrap: scripts/setup_venv.sh). The vendor CLIs import the vendored
# `tradingagents` closure from scripts/vendors/_vendored/ — no external
# checkout or editable install is needed. Creds load from
# ~/.config/tradingagents/*.env (see scripts/vendors/_common.py).
#
# Pins mirror the last-known-good v1 runtime freeze, except pandas:
# 3.0.4 is yanked on PyPI (datetime segfaults), 3.0.3 is the newest safe.

pandas==3.0.3
pydantic==2.13.4
requests==2.34.2
python-dotenv==1.2.2
snaptrade-python-sdk==11.0.212   # imports as `snaptrade_client`
pytest==9.1.1
